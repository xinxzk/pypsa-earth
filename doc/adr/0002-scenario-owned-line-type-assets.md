# Scenario-Owned Line Type Assets

Accepted. Custom line type loading is a generic PyPSA-Earth configuration mechanism, but China-specific line type assumptions live with the China playground scenarios under `playground/config`. This keeps core workflow code region-agnostic while making the provisional China planning assumptions visible next to the scenario configurations that use them.
